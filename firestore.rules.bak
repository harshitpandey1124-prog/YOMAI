rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // PUBLIC READ/WRITE FOR TROUBLESHOOTING
    // DO NOT LEAVE THIS IN PRODUCTION
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
